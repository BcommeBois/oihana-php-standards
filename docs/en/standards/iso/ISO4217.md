# ISO4217

> 🇫🇷 [Version française](../../../fr/standards/iso/ISO4217.md)
>
> | | |
> |---|---|
> | **Standard** | [ISO 4217 — Currency codes](https://en.wikipedia.org/wiki/ISO_4217) |
> | **Namespace** | `org\iso\ISO4217` |
> | **Type** | Constants class (`ConstantsTrait`) |
> | **Source** | [ISO4217.php](https://github.com/BcommeBois/oihana-php-standards/blob/main/src/org/iso/ISO4217.php) |

## Overview

`ISO4217` enumerates ISO 4217 alpha-3 currency codes (`USD`, `EUR`, `JPY`, etc.). The constant name matches the value (e.g. `ISO4217::USD === 'USD'`).

## Sample of common constants

| Code | Currency |
|---|---|
| `USD` | United States Dollar |
| `EUR` | Euro |
| `GBP` | Pound Sterling |
| `JPY` | Japanese Yen |
| `CNY` | Chinese Yuan |
| `CHF` | Swiss Franc |
| `CAD` | Canadian Dollar |
| `AUD` | Australian Dollar |
| `INR` | Indian Rupee |
| `BRL` | Brazilian Real |
| `RUB` | Russian Ruble |
| `KRW` | South Korean Won |
| `MXN` | Mexican Peso |
| `ZAR` | South African Rand |
| `SEK` | Swedish Krona |

> 📋 **Full list** (~180 codes): see the [source code on GitHub](https://github.com/BcommeBois/oihana-php-standards/blob/main/src/org/iso/ISO4217.php).

## Inherited methods

See the [ConstantsTrait](../../guides/constants-trait.md) guide for `getAll()`, `enums()`, `includes()`, `validate()`, `getConstant()`, etc.

## Examples

```php
use org\iso\ISO4217;

ISO4217::USD;                  // "USD"
ISO4217::includes('EUR');      // true
ISO4217::includes('XYZ');      // false
ISO4217::validate('JPY');      // 'JPY'
ISO4217::validate('FAKE');     // throws ConstantException
ISO4217::enums();              // ['AED', 'AFN', ..., 'ZWL']
ISO4217::getConstant('GBP');   // "GBP"
```

### Domain use case — currency input validation

```php
use org\iso\ISO4217;
use InvalidArgumentException;

function validateCurrency(string $code): string
{
    $code = strtoupper($code);
    if (!ISO4217::includes($code))
    {
        throw new InvalidArgumentException("Unknown currency code: $code");
    }
    return $code;
}

validateCurrency('eur'); // "EUR"
validateCurrency('xyz'); // throws
```

## Related

- [`ISO3166_1`](ISO3166_1.md) — countries (often paired with a currency)
- ISO index: [README.md](README.md)

## See also

- [ISO 4217 — Wikipedia](https://en.wikipedia.org/wiki/ISO_4217)
- [ConstantsTrait](../../guides/constants-trait.md)
